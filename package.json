{
  "name": "autonomous-orchestration-agent",
  "version": "1.0.0",
  "description": "Zero-intervention repository synchronization and structural alignment engine.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint src/**/*.ts",
    "build": "npx esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js",
    "start": "node dist/index.js",
    "dev": "npx esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --watch"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.2.0",
    "@typescript-eslint/parser": "^8.2.0",
    "eslint": "^9.9.1",
    "typescript": "^5.5.4",
    "vitest": "^3.2.6"
  },
  "overrides": {
    "esbuild": "^0.25.0",
    "vite": "^6.0.11"
  }
}
